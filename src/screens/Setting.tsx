import {Text, View} from 'react-native';
import {SettingsProps} from '../types/navigationsTypes';

function Setting(props: SettingsProps) {
  return (
    <View>
      <Text>Settings</Text>
    </View>
  );
}
export default Setting;
